Scripts folder for reusable CLI helpers and ETL scripts.

Suggested files to add:
- `scripts/etl_fraud_data.py` — load, clean, and save processed fraud datasets
- `scripts/ip_geolocation.py` — map IP address ranges to country codes
